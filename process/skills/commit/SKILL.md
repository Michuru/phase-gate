---
name: commit
description: Absorbed into /end-task on 2026-09-06 (the next-command-contract redesign) as its narrow-commit depth mode — run that instead.
---

# Commit — absorbed into `/end-task`

Absorbed into **`/end-task`** on 2026-09-06 as its narrow-commit depth mode (Step 5). Run
`/end-task` instead — it surveys once and picks this depth automatically when there's nothing else
pending. This stub is kept for one cycle only, since older references may still say "run `/commit`";
it will be removed once those have had time to go stale naturally.
