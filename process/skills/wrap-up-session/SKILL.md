---
name: wrap-up-session
description: Absorbed into /end-task on 2026-09-06 (the next-command-contract redesign) as its full-sync depth mode — run that instead.
---

# Wrap-up-session — absorbed into `/end-task`

Absorbed into **`/end-task`** on 2026-09-06 as its full-sync depth mode (Step 6). Run `/end-task`
instead — it surveys once and picks this depth automatically when docs have drifted. This stub is
kept for one cycle only, since older references may still say "run `/wrap-up-session`"; it will be
removed once those have had time to go stale naturally.
